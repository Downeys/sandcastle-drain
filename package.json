{
  "name": "sandcastle-drain",
  "private": true,
  "type": "module",
  "bin": {
    "sandcastle": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "drain": "tsx src/cli.ts drain",
    "ship": "tsx src/cli.ts ship",
    "sweep": "tsx src/cli.ts sweep",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"lint: no linter configured\"",
    "test": "vitest run"
  },
  "devDependencies": {
    "@ai-hero/sandcastle": "0.5.7",
    "@types/node": "^22.0.0",
    "execa": "^9.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
